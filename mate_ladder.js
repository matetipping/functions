var ladder = [];
var round_no = tipping_data.round;
var results = find_player_tips(tipping_data.admin, tipping_data.tips);
var tips = tipping_data.tips;
var round_one_tips = find_round_tips("R1", tips) {

var i;
var len = round_one_tips.length;

for (i = 1; i < len; i++) {
    ladder.push([round_one_tips[i][1]]);
}

for (i = 0; i < len; i++) {
    var 
}
